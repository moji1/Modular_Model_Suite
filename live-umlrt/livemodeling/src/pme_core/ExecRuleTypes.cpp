//
//  ExecRuleTypes.cpp
//  pmecore
//
//  Created by Mojtaba Bagherzadeh on 2018-09-14.
//

#include "ExecRuleTypes.hpp"
/*
namespace  pmd {
    <#declarations#>


bool operator==(const pmd::ExecRule &lhs, const pmd::ExecRule &rhs)
 {
     return lhs.loc == rhs.loc && lhs.when == rhs.when && lhs.scope== rhs.scope && lhs.capsule== rhs.capsule;
 }
}
std::size_t hash::operator()(const pmd::ExecRule& k) const
 {
     using std::size_t;
     using std::hash;
     using std::string;
     return ((hash<string>()(k.loc)
          ^ (hash<string>()(k.capsule) << 1)) >> 1)
            ^ (hash<int>()(k.scope) << 1);
 }
*/
